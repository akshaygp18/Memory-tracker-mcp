[project]
name = "memory-tracker-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "akshaygp18", email = "akshaygp41@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=2.0.0",
    "openai>=3.0.0",
]

[project.scripts]
memory-tracker-mcp = "memory_tracker_mcp:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
